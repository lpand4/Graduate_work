spring:
  application:
    name: CommissioningOfVentilationSystems
  datasource:
    url: jdbc:postgresql://localhost:5433/commissioning_of_ventilation_system
    username: root
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

